{
  "name": "@vcode11/pi-codex-fast-mode",
  "version": "0.1.1",
  "description": "OpenAI Fast mode for Pi using the priority service tier and Codex routing hint",
  "type": "module",
  "license": "MIT",
  "author": "Vishal Mishra",
  "repository": {
    "type": "git",
    "url": "https://github.com/vcode11/pi-codex-fast-mode.git"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "openai",
    "codex",
    "fast-mode"
  ],
  "files": [
    "extensions/",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "check": "npm run typecheck && npm test && npm pack --dry-run"
  },
  "pi": {
    "extensions": [
      "./extensions/codex-fast-mode.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.1"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
